# TypeScript

- Model runtime values with accurate types and narrow `unknown` at boundaries; keep compiler checks strict.
- Do not silence type errors with `any`, casts, non-null assertions (`!`), conditional-spread workarounds, or bracket notation. Fix the model or boundary instead.
